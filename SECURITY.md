# Seguridad

Si descubres un problema de seguridad en esta aplicación, contacta con el autor (Carlos Giner Gonzalez / Asesoría Colón) antes de abrirlo en público.

La aplicación solo modifica el almacén de certificados del usuario actual y solo elimina los que el usuario marca y confirma.
